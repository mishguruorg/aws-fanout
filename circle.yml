machine:
  node:
    version: 10.6.0
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - npm install

test:
  override:
    - npm run lint
    - npm run test
